<h2 class="edit-profile">Edit Profile</h1>

<%= render partial: 'form', locals: {submit: 'Edit'} %>
<div class="row justify-content-center">
<button type="button" class="btn btn-danger"><%= link_to "Delete", user_path(@user.id), :method => 'delete' %></button>
</div>
